package org.tnsif.abstractkeyword;

public abstract class Operator {
	
	// abstract method
	 abstract void operation();
	 
	 // if abstract class contain more than one method then provide
	 // the implemention to all the abstract 
	 //method of the class inside the child class
	 
	 //non abstract
	 // concrete 
	 void print (int a,int b) {
		 {
			 System.out.println(a+b);
		 }
		 
	 }

}
